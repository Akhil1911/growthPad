import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    secondary: {
      main: "#588A67",
    },
    darkColor: {
      main: "#254061",
    },
    lightColor: {
      main: "white",
    },
  },
});